<%= render partial: 'bases/base_index', locals: {
  title: title,
  columns: columns,
  collection: collection,
  row_partial: row_partial,
  actions_header: (defined?(actions_header) ? actions_header : nil)
} %>


<!DOCTYPE html>
<html>
  <head>
    <title>ActiveSound</title>
    <%= favicon_link_tag "ActiveSound.png", rel: "icon", type: "image/png" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5 CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      :root { --app-bg-url: url('<%= asset_path "ActiveSound.png" %>'); }
    </style>
  </head>
  <body class="text-light custom-bg">
    <div class="app-overlay">
      <% mostrar_nav = !(usuario_signed_in? && session[:require_password_change] == true && controller_name == "usuarios" && action_name == "edit" && current_usuario.id == @usuario&.id) %>
      <% if mostrar_nav %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-black mb-4">
          <div class="container-fluid">
            <a class="navbar-brand" href="<%= root_path %>">
              <%= image_tag "ActiveSound.png", alt: "ActiveSound Disquería", height: "48", width: "50", class: "d-inline-block align-text" %> ActiveSound
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <%= link_to storefront_productos_path, class: "btn btn-dark btn-sm border-light me-2" do %>
                    StoreFront
                  <% end %>
                </li>
                <% if usuario_signed_in? %>
                  <li class="nav-item">
                    <%= link_to productos_path, class: "btn btn-dark btn-sm border-light me-2" do %>
                      Administración de productos
                    <% end %>
                  </li>
                  <% unless current_usuario.empleado? %>
                    <li class="nav-item">
                      <%= link_to usuarios_path, class: "btn btn-dark btn-sm border-light me-2" do %>
                        Gestión de usuarios
                      <% end %>
                    </li>
                  <% end %>
                  <li class="nav-item">
                    <%= link_to venta_path, class: "btn btn-dark btn-sm border-light me-2" do %>
                      Administración de ventas
                    <% end %>
                  </li>
                <% end %>
              </ul>

              <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <% if usuario_signed_in? %>
                  <li class="nav-item me-2">
                  </li>
                  <li class="nav-item">
                    <%= link_to usuario_path(current_usuario), class: "btn btn-dark btn-sm border-light me-2" do %>
                      <i class="bi bi-person-lines-fill"></i>&nbsp Ver <%= current_usuario.nombre %>
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= button_to destroy_usuario_session_path, method: :delete, class: "btn btn-dark btn-sm border-light" do %>
                      <i class="bi bi-box-arrow-right"></i> Cerrar sesión
                    <% end %>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to new_usuario_session_path, class: "btn btn-dark btn-sm border-light" do %>
                      <i class="bi bi-box-arrow-in-right"></i> Iniciar sesión
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </nav>
      <% end %>
      <div class="container flash-wrapper">
        <% flash.each do |key, value| %>
          <% next if key == "alert" && value.is_a?(String) && value.include?("ya está en uso") %>
          <% css = key == "alert" ? "flash-danger" : "flash-success" %>
          <div class="flash-message <%= css %> mb-2">
            <%= value %>
          </div>
        <% end %>
      </div>

      <main class="container my-4">
        <div class="main-card">
          <%= yield %>
        </div>
      </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <title>TTPS</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5 CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="bg-dark text-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-black mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">TTPS Disquería</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if usuario_signed_in? %>
              <li class="nav-item">
                <%= link_to "Productos", productos_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Usuarios", usuarios_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>

          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <% if usuario_signed_in? %>
              <li class="nav-item me-2">
                <span class="navbar-text text-white">
                  <i class="bi bi-person-circle"></i>
                  <%= current_usuario.nombre %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to destroy_usuario_session_path,
                            data: { "turbo-method": :delete },
                            class: "btn btn-dark btn-sm border-light" do %>
                  <i class="bi bi-box-arrow-right"></i> Cerrar sesión
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_usuario_session_path, class: "btn btn-dark btn-sm border-light" do %>
                  <i class="bi bi-box-arrow-in-right"></i> Iniciar sesión
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container bg-secondary-subtle text-dark rounded-3 p-4">
      <%= yield %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
  </body>
</html>

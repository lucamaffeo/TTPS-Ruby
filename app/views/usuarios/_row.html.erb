<tr>
  <td><%= item.nombre %></td>
  <td><%= item.email %></td>
  <td><%= Usuario::ROLES.key(item.rol)&.to_s&.capitalize || "—" %></td>
  <td>
    <%= link_to usuario_path(item), class: "btn btn-sm btn-as-dark" do %>
      <i class="bi bi-search"></i> Ver
    <% end %>
  </td>
</tr>
